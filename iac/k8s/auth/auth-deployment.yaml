apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
    spec:
      containers:
      - name: auth
        image: scolas3/auth:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
        env:
        - name: PORT
          value: "3001"
        - name: SECRET
          valueFrom:
            secretKeyRef:
              name: general-secrets
              key: secret
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: general-secrets
              key: mongodb_uri
        - name: CLOUD_NAME
          valueFrom:
            secretKeyRef:
              name: general-secrets
              key: cloud_name
        - name: CLOUD_API_KEY
          valueFrom:
            secretKeyRef:
              name: general-secrets
              key: cloud_api_key
        - name: CLOUD_API_SECRET
          valueFrom:
            secretKeyRef:
              name: general-secrets
              key: cloud_api_secret
        readinessProbe:
          httpGet:
            path: /healthz
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3001
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "2Gi"
